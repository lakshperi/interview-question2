DROP TABLE IF EXISTS STORE;

CREATE TABLE STORE( key INT,
                  value varchar(20));

INSERT INTO STORE(key,value ) VALUES
  (0,'1,2,3' );
INSERT INTO STORE(key,value ) VALUES
  (1,'4,5,6' );